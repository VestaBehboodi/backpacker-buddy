name = "backpacker-buddy-api"
main = "worker.js"
compatibility_date = "2026-01-01"

# Secrets (set these with `npx wrangler secret put <NAME>`, never in this file):
#   TRAVELPAYOUTS_TOKEN   recommended — free; powers flights (Aviasales) + hotels (Hotellook)
#   DUFFEL_API_KEY        optional — real-time bookable flight offers; takes priority for flights
